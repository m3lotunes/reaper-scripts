@@ -38,10 +38,14 @@
 }</description>
         <link rel="website">https://cueport.app</link>
       </metadata>
-      <version name="2.5.0" author="CuePort" time="2026-04-18T17:30:00Z">
+      <version name="2.5.2" author="CuePort" time="2026-04-18T18:30:00Z">
        <changelog><![CDATA[
+v2.5.2 — Use Reaper action 43345 to set the time offset (reliable on every
+        Reaper version; GetProjectStateChunk missing on some builds).
+v2.5.1 — Render-start button now actually shifts the ruler (writes PROJOFFS
+        directly in the project chunk; no SWS needed).
 v2.5.0 — One-click "Set render start at cursor" button + visible render-start
-        marker. Auto re-sync after the offset changes. Requires SWS.
+        marker. Auto re-sync after the offset changes.
 v2.4.0 — Respect Reaper project start offset. Set "0:00 to current edit cursor"
         at the render start, re-sync, and markers land on the right ruler time.
 v2.3.2 — Floating menu as separate persistent window (toggle + stays open).
